# supabase/config.toml
# Project-local config for Supabase CLI.

project_id = "lzfgigiyqpuuxslsygjt"

[functions.mirror-url]
verify_jwt = false
# verify_jwt = false because mirror-url has its own URL allowlist + service-role-only side effects.
# This lets pg_net call it from SQL without managing JWT plumbing.
